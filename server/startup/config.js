module.exports = function () {
  /*
    if (!process.env.vidly_jwtPrivateKey) {
        throw new Error('FATAL ERROR: jwtPrivateKey is not defined.');
        process.env.vidly_jwtPrivateKey = "fool";
        console.error( process.env.vidly_jwtPrivateKey);
        process.exit(1);
      }else{
          console.log("etf")
      }*/
  //find the way for this motherfucker
};
